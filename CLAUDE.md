# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Running the code

```
python hello_world.py
```

## Structure

Single-file Python project. All logic lives in [hello_world.py](hello_world.py), which defines a `hello_world()` function and runs it when executed directly.
